import React from "react";

import EuroButton from "./EuroButton";

export default {
  title: "Component/EuroButton",
  component: EuroButton,
};

export const Euro = (): JSX.Element => <EuroButton />;
